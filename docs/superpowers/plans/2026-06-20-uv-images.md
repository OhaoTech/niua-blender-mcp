# Implementation Plan: Subsystem 9 UV / Images

Date: 2026-06-20
Status: completed

## Global Constraints

- Follow TDD: write tests first, run targeted red, implement, run green, commit.
- Preserve existing unwrap/project/pack behavior.
- Keep server specs and add-on commands in parity.
- Mutating tools must set `mutates=True`.
- Context-sensitive UV operators must use `ctx.ensure(active=obj, mode="EDIT", select=[obj])`
  and select all faces before running.

## File Map

- Modify: `src/niua_blender_mcp/domains/uv.py`
- Modify: `blender_addon/niua_mcp_bridge/domains/uv.py`
- Modify: `tests/domains/test_uv.py`
- Modify: `tests/test_smoke_headless.py`
- Modify: `docs/superpowers/specs/2026-06-20-blender-subsystem-roadmap.md`

## Task 1: UV Layer Management

Interfaces:

- `uv.layers(object?)`
- `uv.layer_create(object?, name="UVMap", do_init=True)`
- `uv.layer_set_active(object?, name)`
- `uv.layer_delete(object?, name)`

Steps:

1. Extend fake-bpy UV tests for layer create/set/delete and router surface.
2. Run targeted red tests.
3. Add server specs and add-on handlers.
4. Run:
   - `pytest tests/domains/test_uv.py tests/test_parity.py -v`
5. Commit:
   - `git commit -m "feat: add uv layer management"`

## Task 2: Seam Report And Mutation

Interfaces:

- `uv.seams(object?)`
- `uv.set_seams(object?, edges="", action=SET|ADD|REMOVE|CLEAR)`

Steps:

1. Extend fake-bpy UV tests:
   - seam report returns marked edge indices
   - SET, ADD, REMOVE, CLEAR actions mutate `edge.use_seam`
   - invalid edge index raises `invalid_params`
2. Run targeted red tests.
3. Add server specs and add-on handlers.
4. Run:
   - `pytest tests/domains/test_uv.py tests/test_parity.py -v`
5. Commit:
   - `git commit -m "feat: add uv seam controls"`

## Task 3: UV Layout Export

Interfaces:

- `uv.export_layout(object?, path, size=1024, opacity=0.25, export_all=True, modified=False)`

Steps:

1. Extend fake-bpy UV tests:
   - export enters edit mode, selects all, calls `uv.export_layout` with filepath,
     size tuple, opacity, export_all, and modified.
2. Run targeted red test.
3. Add server spec and add-on handler.
4. Run:
   - `pytest tests/domains/test_uv.py tests/test_parity.py -v`
5. Commit:
   - `git commit -m "feat: add uv layout export"`

## Task 4: Live Smoke, Roadmap, Final Verification

Steps:

1. Add `test_uv_images_workflow` to `tests/test_smoke_headless.py`.
2. Update roadmap: subsystem 9 complete, next subsystem 10 Animation / Rigging.
3. Run:
   - `pytest tests/test_smoke_headless.py::test_uv_images_workflow -v`
   - `pytest tests/test_smoke_headless.py -v`
   - `pytest`
   - `python -c "from niua_blender_mcp.domains import build_router; names={s.name for s in build_router().specs()}; required={'uv.layers','uv.layer_create','uv.layer_set_active','uv.layer_delete','uv.seams','uv.set_seams','uv.export_layout'}; print(required <= names)"`
   - `git status --short --branch`
4. Commit:
   - `git commit -m "test: cover uv images workflow"`
